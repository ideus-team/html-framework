default:
  description: 'Default (production) build'
  tasks:
    - prod
img:
  description: 'Image tasks'
  tasks:
    - 'concurrent:imgMin'
prod:
  description: 'Production build'
  tasks:
    - clean
    - 'concurrent:prod_css'
    - 'concurrent:prod_js'
    - img
